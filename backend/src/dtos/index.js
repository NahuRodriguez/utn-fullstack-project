const { userDTO } = require("./user.dto");
const { productDTO } = require("./product.dto");
const { orderDTO } = require("./order.dto");
const { categoryDTO } = require("./category.dto");
const { addressDTO } = require("./address.dto");

module.exports = { userDTO, productDTO, orderDTO, categoryDTO, addressDTO };
