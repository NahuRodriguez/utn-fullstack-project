info:
  name: Restore Address
  type: http
  seq: 6

http:
  method: PATCH
  url: "{{PROTOCOL}}{{BASE_URL}}{{PORT}}{{ADDRESSES_API}}restore/{{ADDRESS_ID}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
