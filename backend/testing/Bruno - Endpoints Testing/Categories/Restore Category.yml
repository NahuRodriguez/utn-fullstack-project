info:
  name: Restore Category
  type: http
  seq: 6

http:
  method: PATCH
  url: "{{PROTOCOL}}{{BASE_URL}}{{PORT}}{{CATEGORIES_API}}restore/{{CATEGORY_ID}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
