info:
  name: Get Category
  type: http
  seq: 2

http:
  method: GET
  url: "{{PROTOCOL}}{{BASE_URL}}{{PORT}}{{CATEGORIES_API}}{{CATEGORY_ID}}"
  auth: inherit

runtime:
  variables:
    - name: NONDESTRUCTIVE_CATEGORY_ID
      value: 69d5f25be14fa9903bb73bfd

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
