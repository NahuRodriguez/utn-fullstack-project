info:
  name: POST products
  type: http
  seq: 3

http:
  method: POST
  url: "{{PROTOCOL}}{{BASE_URL}}{{PORT}}{{PRODUCTS_API}}"
  params:
    - name: name
      value: i3 9100f
      type: query
      disabled: true
    - name: price
      value: "95000"
      type: query
      disabled: true
    - name: stock
      value: "30"
      type: query
      disabled: true
  body:
    type: json
    data: |-
      {
        "name": "i3 10100f",
        "price": 95000,
        "stock": 30
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
