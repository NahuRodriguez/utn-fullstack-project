info:
  name: GET orders
  type: http
  seq: 1

http:
  method: GET
  url: "{{PROTOCOL}}{{BASE_URL}}{{PORT}}{{ORDERS_API}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
