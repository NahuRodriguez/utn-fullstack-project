info:
  name: Get Order
  type: http
  seq: 2

http:
  method: GET
  url: "{{PROTOCOL}}{{BASE_URL}}{{PORT}}{{ORDERS_API}}{{ORDER_ID}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
