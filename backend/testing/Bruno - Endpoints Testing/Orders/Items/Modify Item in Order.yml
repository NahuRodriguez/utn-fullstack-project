info:
  name: Modify Item in Order
  type: http
  seq: 2

http:
  method: PUT
  url: "{{PROTOCOL}}{{BASE_URL}}{{PORT}}{{ORDER_ITEMS_API}}{{ORDER_ID}}/{{ORDER_ITEM_ID}}"
  body:
    type: json
    data: |-
      {
        "productId": "asdas",
        "quantity": 4
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
