info:
  name: Hard-Delete Item in Order
  type: http
  seq: 1

http:
  method: DELETE
  url: "{{PROTOCOL}}{{BASE_URL}}{{PORT}}{{ORDER_ITEMS_API}}{{ORDER_ID}}/{{ORDER_ITEM_ID}}"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
