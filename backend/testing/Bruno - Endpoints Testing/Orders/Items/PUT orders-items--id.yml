info:
  name: PUT orders/items/:id
  type: http
  seq: 2

http:
  method: PUT
  url: "{{PROTOCOL}}{{BASE_URL}}{{PORT}}{{ORDER_ITEMS_API}}{{NONDESTRUCTIVE_ORDER_ITEM_ID}}"
  body:
    type: json
    data: |-
      {
        "productId": "asdas",
        "quantity": 4
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
