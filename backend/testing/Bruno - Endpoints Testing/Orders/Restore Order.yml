info:
  name: Restore Order
  type: http
  seq: 6

http:
  method: PATCH
  url: "{{PROTOCOL}}{{BASE_URL}}{{PORT}}{{ORDERS_API}}restore/{{ORDER_ID}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
