info:
  name: POST payments
  type: http
  seq: 3

http:
  method: POST
  url: "{{PROTOCOL}}{{BASE_URL}}{{PORT}}{{PAYMENTS_API}}"
  params:
    - name: name
      value: i3 9100f
      type: query
      disabled: true
    - name: price
      value: "95000"
      type: query
      disabled: true
    - name: stock
      value: "30"
      type: query
      disabled: true
  body:
    type: json
    data: |-
      {
        "amount": 100000
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
