info:
  name: GET users/:id
  type: http
  seq: 2

http:
  method: GET
  url: "{{PROTOCOL}}{{BASE_URL}}{{PORT}}{{USERS_API}}{{NONDESTRUCTIVE_USER_ID}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
