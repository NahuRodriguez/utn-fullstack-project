info:
  name: Restore User
  type: http
  seq: 6

http:
  method: PATCH
  url: "{{PROTOCOL}}{{BASE_URL}}{{PORT}}{{USERS_API}}restore/{{USER_ID}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
